<?xml version="1.0" encoding="utf-8"?>

<!-- Note: the actual manifest file used in your APK merges this file with contributions
     from the modules on which your app depends (such as FtcRobotController, etc).
     So it won't ultimately be as empty as it might here appear to be :-) -->

<!-- The package name here determines the package for your R class and your BuildConfig class -->
<manifest package="org.firstinspires.ftc.teamcode"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- We have to override android:name property that is defined in the FtcRobotController
    manifest in order ot use it for access to the AppContextUtility we use to get to the
    Assets folder
    <application
        tools:replace="android:name"
        android:name=".util.AppContextUtility"
        />
        // Unfortunately this conflicts with other FtcRobotController code
    -->

</manifest>
