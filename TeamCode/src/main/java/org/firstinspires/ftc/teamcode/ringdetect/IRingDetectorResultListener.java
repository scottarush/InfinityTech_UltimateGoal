package org.firstinspires.ftc.teamcode.ringdetect;

/**
 * Callback listener interface for an averaging listener result.
 */
public interface IRingDetectorResultListener {
    void averageDetectionResult(int result);
}
